{
  "name": "meeting-web-root",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install",
    "start": "npm --prefix backend start"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}